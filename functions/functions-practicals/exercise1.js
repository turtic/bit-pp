// Write a function that calculates the maximum of two given numbers. 

function numberMax(a, b) {

    // solution 1:
    var max;

    (a > b) ? max = a : max = b

    return max;

    // solution 2:

    // if (a > b) {
    //     return a
    // } else {
    //     return b
    // }

}

console.log(numberMax(-1, -110));